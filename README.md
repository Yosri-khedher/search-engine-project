# search-engine-project
Mini search engine developed for Multimedia Indexing course. Implements inverted index, TF-IDF with cosine similarity, and Boolean model (AND/OR/NOT). Includes a Streamlit web interface, AI query suggestions, and optional image search. Deployed online for easy access.
